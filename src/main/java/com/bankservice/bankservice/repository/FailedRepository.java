package com.bankservice.bankservice.repository;

import com.bankservice.bankservice.entity.Failed;
import org.springframework.data.repository.CrudRepository;

public interface FailedRepository extends CrudRepository<Failed, Long> {
}
